{% extends 'SandboxCoreBundle:Backend:layout.html.twig' %}

{% block header %}
    <h1>Address updating <small>Use this form to edit existing address</small></h1>
{% endblock %}

{% block content %}
<form action="{{ path('sylius_sandbox_backend_address_put', {'id': address.id}) }}" method="post" class="form-horizontal" novalidate>
    {% include 'SandboxCoreBundle:Backend/Address:_form.html.twig' %}
    <div class="form-actions">
       <input type="submit" value="Save changes" class="btn btn-large btn-primary" />
       &nbsp;
       <a href="{{ path('sylius_sandbox_backend_address', {'id': address.id})}}" class="btn btn-large">Cancel</a>
    </div>
</form>
{% endblock %}
